// Ciclos repiten, recorre una lista

// Sintaxis for
//for(inicio;condicion; comoavanza){}

for (let i=0; i < 5; i++){
    console.log("vuelta numero "+ i);
}

// for of para listas
const numeros = [0,1,2,3,4,5];
for (const numero of numeros) {
    console.log("Numero: "+numero);
}